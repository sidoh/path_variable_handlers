language: python
python:
  - "2.7"

# Cache PlatformIO packages using Travis CI container-based infrastructure
sudo: false
cache:
  directories:
    - "~/.platformio"

install:
  - pip install -U platformio
  - platformio update
  - platformio lib -g install 'ESP Async WebServer'

script:
  - PLATFORMIO_CI_SRC=examples/esp8266_simple platformio ci --board=d1_mini --lib="."
  - PLATFORMIO_CI_SRC=examples/esp32_simple platformio ci --board=esp32doit-devkit-v1 --lib="."